<div class="container">
<div class="row">
<h1><%= @room.name %></h1>

<h4>参加者</h4>
<% @user_rooms.each do |e| %>
  <%= link_to e.user.name, user_path(e.user_id) %>
<% end %>

<h1>Chat room</h1>
<form>
  <label>Say something:</label><br>
  <input type="text" id="chat-input" data-behavior="room_speaker">
</form>

<div id="chats" data-room_id="<%= @room.id %>">
  <%= render partial: 'chats/chat', chats: @chats %>
</div>
</div>
</div>
<script>
$(document).on("turbolinks:load", function() { App.room.unsubscribe(); }); 
</script>